// package.json
{
  "name": "conecta-poste-api",
  "version": "1.0.0",
  "description": "API para mapear postes usando Neon (PostgreSQL) no Vercel",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "pg": "^8.10.0"
  }
}
